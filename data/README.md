# Required data files

The simulator expects course-provided AlexNet weights, biases, ImageNet class labels, and input files under this directory. These files are not redistributed in this repository.

See `ROM.cpp`, `ROM.h`, and `Pattern.cpp` for the expected filenames and loading behavior.
